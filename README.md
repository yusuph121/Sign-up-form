# sign-up-form
A Responsive Sign Up form is used to enter authentication credentials to access a restricted page or form. The Sign Up form contains a field for the username , another for the password and confirm password. These settings specify the database tables and fields that are used during the authentication process.
